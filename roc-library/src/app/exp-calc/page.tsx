import ExpCalculator from './ExpCalculator';

export const metadata = {
  title: 'Exp Calculator | ROC Library',
  description: 'Calculate experience needed for leveling up across different classes',
};

export default function ExpCalcPage() {
  return <ExpCalculator />;
}
